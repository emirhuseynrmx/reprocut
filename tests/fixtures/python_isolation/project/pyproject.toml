[build-system]
requires = []
build-backend = "backend"
backend-path = ["."]

[project]
name = "reprocut-isolation-fixture"
version = "1.0.0"
dependencies = ["required-dep==1.0.0", "unused-dep==1.0.0"]
